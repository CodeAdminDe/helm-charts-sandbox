apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webfinger.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "webfinger.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  {{- if .Values.runtimeClass.tests }}
  runtimeClassName: {{ .Values.runtimeClass.tests }}
  {{- end }}
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:
        - '-qO-'
        - 'http://{{ include "webfinger.fullname" . }}:{{ .Values.service.port }}/.well-known/webfinger?resource={{ .Values.webfingerRessource }}'
  restartPolicy: Never
